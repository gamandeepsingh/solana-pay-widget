import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* If u got the "buffer" or "process" error then paste the nextJS config code here present in the QUICKSTART.md */
};

export default nextConfig;
